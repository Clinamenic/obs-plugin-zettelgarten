# Version bump configuration for Obsidian plugin starter
# Uncomment and modify variables to override script defaults.
# This file is sourced by .workspace/scripts/version-bump.sh

# Require a clean git working tree before bumping. Recommended for releases.
# REQUIRE_CLEAN_TREE=true

# Paths (defaults shown; change only if your layout differs)
# MANIFEST_PATH="$REPO_ROOT/manifest.json"
# PACKAGE_JSON_PATH="$REPO_ROOT/package.json"
# VERSIONS_JSON_PATH="$REPO_ROOT/versions.json"
# DOAP_JSON_PATH="$REPO_ROOT/doap.json"

# Optional hook scripts (executed if present)
# PRE_HOOK="$REPO_ROOT/.workspace/scripts/version-bump.pre.sh"
# POST_HOOK="$REPO_ROOT/.workspace/scripts/version-bump.post.sh"
